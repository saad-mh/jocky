# JOCKY Application — Python dependencies
#
# Install all at once:
#   pip install -r requirements.txt
#
# Python 3.10+ required.

# Core compiler requirement — LLVM IR builder + JIT engine
llvmlite>=0.39.0

# Rich terminal UI (optional but strongly recommended)
rich>=13.0.0
