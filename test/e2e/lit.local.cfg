# The end-to-end tests compile a .jk program to a real executable and run it.
# That needs a working linker (a `clang` for JOCKY to shell out to). If none was
# found at configure time, skip this whole directory instead of failing.
if "e2e" not in config.available_features:
    config.unsupported = True
