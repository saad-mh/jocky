# Test wiring for JOCKY.
#
# The real lit + FileCheck setup is added in the next commit (alongside the
# first lexer tests). For now this file only exists so the top-level
# `add_subdirectory(test)` has something to point at.

# Placeholder: no tests are registered yet.
