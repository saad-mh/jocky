// RUN: %not %jocky parse "%s" 2>&1 | %FileCheck "%s"
//
// The parser recovers after each error and keeps going, so one run reports
// several distinct problems and then stops (it does not loop).

let x = 1 +;
let y = (2 * 3;
func () { return 1; }
print(x

// CHECK-DAG: :6:12: error: expected an expression but found ';'
// CHECK-DAG: :7:15: error: expected ')' to close the parenthesized expression but found ';'
// CHECK-DAG: :8:6: error: expected a function name after 'func'
